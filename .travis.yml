#set up by Nephi
#validate this file here: http://lint.travis-ci.org/

language: cpp
compiler:
  - g++
  #- clang
# Change this to your needs
#script: ./configure && make
script: make clean && make && PacMan-Clone.exe #./build/PacMan-Clone.exe
